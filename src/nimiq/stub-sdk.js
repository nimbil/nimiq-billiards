// Stub for @nimiq/mini-app-sdk
// The real SDK is loaded at runtime when running inside a Nimiq Mini App.
// In standalone mode, wallet features are unavailable.
export async function init() {
  return null;
}
